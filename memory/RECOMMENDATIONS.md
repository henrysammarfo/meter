# METER — Recommendations (raise Track A win chance)

Prioritized by impact before **2026-09-08 23:59 UTC**. Complexity noted technically, not in calendar days.

## P0 — Contest artifacts

1. **Record the five-beat live demo** (fund → 402 → paid research → invoice → limits) against production `/api/v1`.  
2. **GitHub README** with architecture diagram + deletion test.  
3. **X quote + survey** exactly per Binance rules (geo-check first).

## P0 — Settlement wow

4. Supply **Binance Agent OS / x402 facilitator** credentials → flip on-chain settle; keep prepaid as sandbox mirror.  
5. Show **batching under $20/day** in UI copy and a limit-trip in the video.

## P1 — Providers

6. **Rotate all keys** pasted in chat.  
7. Fund TinyFish Agent wallet **or** stay on Search/Fetch (already enough for research).  
8. Unblock LLM: Venice key **or** AgentRouter from non-WAF egress — optional for synthesis, not required for settle demo.

## P1 — Product sharpness vs peers

9. One screen that proves **both agents see the same receipt id**.  
10. Public rate card only for `/research` live; keep other endpoints paused until real handlers exist (no fake live badges).

## P2 — Hardening (honest, not “unhackable”)

11. Move ledger to managed store (D1/Postgres) before multi-instance deploy.  
12. Add request auth for operator APIs (fund/invoice) separate from agent pay headers.  
13. Threat model doc: key theft, facilitator malice, prompt injection, ledger tampering.

## Bible adjustments

- Prefer TechCrunch-verified **$20/day x402** language over vague “batch somehow”.  
- Drop BSC-as-default until proven with credentials.  
- Keep BRAKE/trading on the kill list.
